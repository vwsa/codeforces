#include <bits/stdc++.h>
using namespace std;
// b = aq + r (a != 0, 0 <= r < |a|), q,r is unique when a, b is given.
void calculate(int d) {
    // b = aq' + r'
    // d <= r' < d + |a|, get r' and q'
     
}  